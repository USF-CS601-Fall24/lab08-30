package interfaces.movable;

/** The interface has a move method - it means objects of whatever class implements this interface
 * can move.
 */
public interface Movable {
        void move(int distance);
}
