package interfaces.movable;

public class PolylineTest {
    public static void main(String[] args) {
        Polyline polyline1 = new Polyline();

        // Add several points to polyline1 (make up some x, y coordinates)
        polyline1.addPoint(10, 10);
        polyline1.addPoint(20, 50);
        polyline1.addPoint(100, 300);

        // FILL IN CODE: call move(5) on the polyline1 to shift each point by 5 units to the right and 5 units up
        // print the polyline
    }
}
